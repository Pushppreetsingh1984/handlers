---
- hosts: all
  tasks:
          - copy:
              content: "welcome to aus/nx"
              dest: /tmp/newzeland
            notify: restart_apache
  handlers:
          - name: restart_apache
            service:
              name: httpd
              state: restarted
              enabled: yes

              
...
